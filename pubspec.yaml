name: yms_mobile_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  # Paquets HTTP & État
  http: ^1.2.0
  dio: ^5.11.0
  provider: ^6.1.5+1
  flutter_secure_storage: ^11.0.0

  # PDF & Fichiers (Sprint 3 - MOB-A3)
  path_provider: ^2.1.2
  flutter_pdfview: ^1.3.2

  # Icônes
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # Décommente cette section après avoir ajouté l'image du logo dans assets/images/
  # assets:
  #   - assets/images/logo.png