import 'package:flutter/material.dart';

class FormateurShell extends StatelessWidget {
  const FormateurShell({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Espace Formateur'),
      ),
    );
  }
}